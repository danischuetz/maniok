# Workflow to build and publish Docker image for Dockerfile.Editing
name: Build & Publish Editing Image

on:
  workflow_dispatch:

jobs:
  build-and-push:
    permissions:
      contents: read
      packages: write
    uses: ./.github/workflows/docker-publish.yml
    with:
      dockerfile: Dockerfile.Editing
      image_name: editing
